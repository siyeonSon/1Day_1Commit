#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, l;
    cin >> n >> l;
    vector<int> vec(n);
    for (int i = 0; i < n; i++) cin >> vec[i];

    deque<int> deq;
    if (deq.size() < l) {
        
    } 

    return 0;
}